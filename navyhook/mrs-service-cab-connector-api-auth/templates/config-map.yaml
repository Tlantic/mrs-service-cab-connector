apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fullname" . }}
data:
  {{ template "fullname" . }}: |
    Name: {{ template "fullname" . }}
    Ports:
      auth: :{{ .Values.service.ports.grpc.externalPort }}
      apihttp: :{{ .Values.connectorApi.ports.http.externalPort }}
      apigrpc: :{{ .Values.connectorApi.ports.grpc.externalPort }}
    External:
      BaseURI: {{ .Values.service.external.baseURI }}
      Resources:
        Auth:
          Path: {{ .Values.service.external.resources.auth.path }}
    Ports:
      grpc: :{{ .Values.service.ports.grpc.externalPort }}